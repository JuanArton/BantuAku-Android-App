<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.TadpoleStudio.BantuAku">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/applicationicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/applicationicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".backtomain"></activity>
        <activity android:name=".searchResult" />
        <activity android:name=".Register" />
        <activity android:name=".detail_profile" />
        <activity android:name=".NameInput" />
        <activity android:name=".MainMenu" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>